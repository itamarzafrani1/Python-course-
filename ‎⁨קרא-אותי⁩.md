# מסלול פייתון — מהיסודות לבקאנד ולבדיקות

20 מודולים · 80 שיעורים · 80 חידונים · 30 תרגילים ניתנים להרצה · מגרש משחקים

## הקבצים
| קובץ | תפקיד |
|---|---|
| `index.html` | האפליקציה כולה — קובץ יחיד, עובד גם לבד |
| `manifest.webmanifest` | מאפשר התקנה למסך הבית |
| `sw.js` | שומר את האפליקציה במטמון לעבודה אופליין |
| `icon-192.png`, `icon-512.png` | האייקון |

## איך להוסיף למסך הבית

התקנה למסך הבית דורשת שהקובץ יוגש מכתובת (URL), לא מהקבצים המקומיים. זו דרישה של iOS ואנדרואיד, ולכן צריך פעם אחת להעלות את התיקייה לאירוח חינמי.

### הדרך המהירה — Netlify Drop (2 דקות, בלי הרשמה)
1. מהמחשב, היכנס ל-https://app.netlify.com/drop
2. גרור לשם את **התיקייה כולה** (כל 5 הקבצים יחד)
3. תקבל כתובת כמו `https://random-name.netlify.app`
4. פתח אותה בנייד ← כפתור השיתוף ← **הוסף למסך הבית**

### חלופה — GitHub Pages
צור מאגר, העלה את הקבצים, ואז Settings ← Pages ← Deploy from branch ← main / root.

אחרי ההתקנה האפליקציה נפתחת במסך מלא, בלי סרגלי דפדפן, ועובדת גם בלי אינטרנט.

## הרצת קוד

יש שלוש דרכים לכתוב ולהריץ קוד, כולן בדפדפן דרך Pyodide:

| איפה | מה עושים |
|---|---|
| **9 מעבדות pytest** | כותבים מימוש או בדיקה, ואנחנו מריצים עליה pytest אמיתי. בחמש מהן יש גם בדיקת מוטציה: הבדיקה שלך מורצת מול קוד עם באג מכוון, כדי לוודא שהיא באמת תופסת אותו |
| **8 תרגילי פלט** | כותבים קוד, מריצים, ומשווים את הפלט לצפוי — עם תצוגת השוואה שורה מול שורה |
| **מגרש משחקים** (כפתור `>_` למעלה) | קוד חופשי במצב סקריפט, או כתיבת בדיקות במצב pytest. שש תבניות פתיחה מוכנות |

- **עובד**: כשפותחים את הקובץ בדפדפן רגיל, או מהכתובת המאוחסנת.
- **לא עובד**: בתוך תצוגה מוטמעת של אפליקציה אחרת — היא חוסמת WebAssembly. במקרה כזה תקבל במקום זה בדיקה מבנית של הקוד.

ההרצה הראשונה מורידה את Pyodide (עשרות MB) ולוקחת 20–40 שניות. אחריה זה מיידי, וה-service worker שומר אותו לשימוש אופליין.

### אופליין מוחלט מהרגע הראשון
הורד את `pyodide-*.tar.bz2` מ-https://github.com/pyodide/pyodide/releases, חלץ לתיקייה בשם `pyodide` ליד `index.html`. האפליקציה מעדיפה אותה על פני הרשת.

## מקשים
`/` חיפוש · `←` השיעור הבא · `→` הקודם · `Ctrl+Enter` הרצת התרגיל · `Tab` הזחה בעורך
